# NOTE: org.graalvm.buildtools.native 0.10.6 uses DefaultLegacyConfiguration which
# Gradle 9.x cannot serialize. Configuration cache is disabled until a compatible
# plugin version is available.
# org.gradle.configuration-cache=true

# Other recommended optimizations
org.gradle.caching=true
org.gradle.parallel=true

# GraalVM JDK path for native image compilation (installed via asdf)
org.gradle.java.installations.paths=/Users/urgoringo/.asdf/installs/java/graalvm-community-25.0.2
