spring:
  application:
    name: mealkit

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  modulith:
    moments:
      enable-time-machine: false
      locale: en_US
      zone-id: UTC
      granularity: days
